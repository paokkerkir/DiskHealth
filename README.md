# DiskHealth
Windows PowerShell GUI tool that uses smartctl to enumerate physical disks, evaluate SSD/HDD health from SMART data, display per-disk status in a WPF popup ordered by drive letter, and write a dated health log.
